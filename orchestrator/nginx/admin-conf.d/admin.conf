 server {
  listen 80 default_server;
  server_name _;

  # Let’s Encrypt HTTP-01 challenge
  location ^~ /.well-known/acme-challenge/ {
    root /var/www/acme-challenge;
    try_files $uri =404;
  }

  # Optional: basic health page
  location = /health { return 200; }
}
