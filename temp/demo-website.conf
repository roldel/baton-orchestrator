{
  # Optional: Global settings (e.g., email for Let's Encrypt)
  # email john@exmaple.com
  # acme_dns cloudflare API_KEY  # Only needed if using DNS challenge
}

example.com www.example.com app.example.com api.example.com {
  # Static files
  handle_path /static/* {
    root * /shared-files/example.com
    file_server
  }

  # Media files
  handle_path /media/* {
    root * /shared-files/example.com
    file_server
  }

  # Reverse proxy to your app
  reverse_proxy http://myapp-service:8000
}
